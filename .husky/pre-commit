#!/usr/bin/env sh

# Validate ADR decision records
pnpm tsx scripts/validate-decisions.ts --validate

# Format and lint with Biome (only staged files)
pnpm biome check --staged --no-errors-on-unmatched
